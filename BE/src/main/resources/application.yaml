server:
  port: 8080

spring:
  application:
    name: qbmp-api
  datasource:
    url: jdbc:mysql://localhost:3306/finflow_database
    username: root
    password: duyhai123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  flyway:
    baseline-on-migrate: true